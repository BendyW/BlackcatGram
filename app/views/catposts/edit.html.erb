<div class="text-center">
  <%= image_tag @catpost.image.url(:medium) %>
</div>

<%= render 'form'%>

<div class="text-center edit-links">
  <%= link_to 'Delete Post', catpost_path(@catpost), method: :delete,
              data: {confirm: "Are you sure you want to delete this cat post?"}%>
  |
  <%= link_to "Cancel", catposts_path %>
</div>
